module.exports = {
    rules: {
        create: {
            title: "#title:",
            desc: "#desc:",
            category_id: "#cat_id:", // optional
            img_url: "#img_url:", // optional
            position: "#pos:" // optional
        }
    }
};