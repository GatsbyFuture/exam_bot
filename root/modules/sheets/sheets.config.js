module.exports = {
    rules: {
        create: {
            title: "#title:",
            desc: "#desc:",
            category_id: "#category:", // optional
            img_url: "#img_url:", // optional
            position: "#pos:" // optional
        }
    }
};